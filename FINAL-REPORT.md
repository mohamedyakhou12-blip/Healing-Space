# FINAL-REPORT — تدقيق وإصلاح موقع Healing Space

تاريخ: 2026-09-21

## الملخص
تم تدقيق وإصلاح المشروع شاملةً (Next.js App Router، React 19، TypeScript، Firebase Firestore + Admin، جلسات iron-session، RTL عربي) لضمان أمان حقيقي ووظائف صحيحة، مع الحفاظ على **الحد الأدنى من تغيير الملفات** وعدم حذف أي ميزة، وعدم لمس أي بيانات أو schema. اللمسة البصرية الموحّدة أصبحت بيضاء/بيج/بنية (بدون أخضر) كما طلب المالك.

## نتائج الفحص النهائي
- `npx tsc --noEmit`: **نظيف (rc=0)**.
- `npm run lint` (`eslint .`): **27 errors + 2 warnings في 11 ملفاً** — نفس البازلاين قبل التغييرات تماماً، لم نضف أي خطأ جديد.
- `npm run build`: **نجح (rc=0)** بدون أي خطأ compile.
- المسارات الديناميكية `/courses/[id]` و`/articles/[id]` حقيقية على الخادم (ƒ Dynamic) وتتفوق على rewrites في vercel.json (أولوية الملفات في Vercel) → كل محتوياتك مفتوحة داخل الموقع.

## تغطية البنود الستة

### 1) كل المحتوى يُفتح داخل الموقع ✓
- أزرار ميتة في القوائم موصولة بصفحات تفاصيل حقيقية: `CoursesPage.tsx` (Enroll/Continue/Completed ← `/courses/[id]` عبر `<Link>`+`<Button asChild>`). `CoachingPage` يستبدل زر "ابدأ الآن" الميت بشارة حالة واضحة "متاح لك" (فرنسي/إنجليزي).
- صفحات التفاصيل `articles/[id]` و`courses/[id]` أُعيد بناؤها وتعمل (تقرأ من API، تعرض paywall لغير المشتركين، حرفياً `not-found` عند غياب id).

### 2) الاشتراك شهر كامل ✓
- `expiresAt = setMonth(now.getMonth() + 1)` عند قبول الدفعة؛ دخول المشترك يُفحص فعلياً في كل الطلبات (user-access). تجديد/استمرارية تُعاد حسب تاريخ الانتهاء من DB لا جلسة.
- في `admin`, الموافقة على دفعة تُفعّل الاشتراك فوراً وتُبطل ثم تُعاد بيانات القوائم.

### 3) الأسعار من مصدر واحد وتظهر فوراً بعد التغيير ✓
- `/api/subscription-prices` في Firestore هو **المصدر الوحيد**: صفحة الدفع تجلب منه مباشرة في كل تحميل (لا `localStorage`).
- تبويب "الأسعار" في لوحة الأدمن يعدّل نفس المستند، فتظهر قيمة جديدة في صفحة الدفع فوراً (لا تحقق عميل، لا كاش ثابت).

### 4) تحقق خادمي من الأسعار ✓
- `payments/route.ts` و`purchases/route.ts` لا يقبلان السعر من العميل: يتحققان من أسعار DB عبر `resolveContentItem` وقيود `REQUEST_LIMITS`؛ أي خطة منفردة (أي سعر ← يطلب اشتراك شهر) تُرفض بـ `403`.
- الحدود (MIN/MAX price، عدد الأحرف) مطبقة خادمياً عبر zod.

### 5) حجب محتوى حقيقي خادمي ✓
- `gateContentItem`/`gateContentList` في `api-content-gate` تزيل المحتوى المميز (body/videoUrl/audioUrl/….) للمستخدمين غير المشتركين أو غير المسجلين — الحجب فعلي على الخادم لا CSS.
- مشروع/مقال/فيديو/بودكاست/PDF/جلسة مباشرة غير المميزة تعمل لكل الزوار.
- قواعد الأمان خادميّة موجودة في `/staff/local.firestore.rules` + توقيعات Cloudinary خادمية.
- **جديد: سد تسريب المسودات** — القوائم (articles/videos/podcasts/pdfs/live/courses/coachings) كانت تعيد كل السجلات عندما لا يُمرّر `?status=`؛ الآن أي طلب غير مسؤول يُقصَر على `published` تلقائياً (مع `verifyAdminAccess` غير الرامي). الإداري يستطيع طلب `?status=draft` برأس/جلسة صالحة فقط.

### 6) كل شيء قابل للتعديل من الأدمن ✓
- وحّدنا اللوحة على `/admin?tab=…` (تبويبات: dashboard/content/members/payments/prices/homepage/settings/purchases) بشرائح حقيقية متصلة بواجهات API فعلية.
- حذفنا 5 صفحات موك ثابتة (`members/content/customize/settings/payments` تحت `src/app/admin`) — كلها موحَّدة في `AdminPage.tsx` مع بيانات حية من Firestore.
- `/api/admin/all-content` و`/api/admin/stats` و`thenew prices/settings/homepage-images/sliders` تغذّي أقسام الأدمن؛ كل عمليات الحفظ/الحذف تمر عبر وسائط مصادقة (`verifyAdminAccess`).
- حساساً: صفحتا "الإعدادات" و"التشخيص" في الأدمن لا تكشفان Service Account أو مفاتيح Firestore.

## إزالة الـ Mock و localStorage
- أزلنا المصفوفات الثابتة من صفحات المحتوى (Videos/Podcasts/Pdfs/Live/Books/AllContent/…)، وأنواع `!= null` بدل `=== null` السذجة، وواجهات Empty State حقيقية.
- `PaymentPage` لا يقرأ/يكتب `hs_subPrices`; `client-cache` ذاكرة فقط (لا تخزين مستمر).
- **لا بيانات حساسة في العميل**: `firebase.ts` مجرد إعدادات عامة عام; `.env` غير مرفوع (يوصى بتدويره قبل النشر الرسمي لأن مسودات قديمة لعرضت محلياً).
- `session.ts` الآن **fail-closed في الإنتاج**: غياب `SESSION_SECRET` (≥32 حرفاً) يُلقي خطأ ويمنع البدء — أُزلنا backdoor الاشتقاق من Service Account والـ fallback الثابت.

## إصلاحات تقنية إضافية (هذه الجلسة)
- إزالة 3 أخطاء lint جديدة كانت بعد دمج الأدمن:
  - `admin/layout.tsx`: استُبدل `useEffect`+`useState` لقراءة `?tab=` بـ `useSearchParams` داخل مكوّن داخلي مغلّف بـ `<Suspense>` (لا setState في effect، لا hydration mismatch).
  - `articles/[id]` و`courses/[id]`: أزيلت setState المتزامنة في effect، وأصبحت حالة `not-found` تُشتق عند غياب `id`.
- إزالة استيرادات `requireAdmin` غير المستخدمة من القوائم 7.
- شطف أخضر بصري بالكامل نحو بيج/بني (globals.css، tailwind.config، الحراس في الصفحات) دون المساس بأي حقول بيانات تحمل "green".
- ترميم كامل للملفات الأربعة من مجلد skills بعد ضرر جانبي، والتحقق.

## مصفوفة الامتثال للمواصفات الكاملة (هذه الجلسة)
تدقيق شامل لكل تجربة المستخدم والأدمن مقابل المواصفات، ثم إصلاح الثغرات المؤكدة. كل سطر {✓ يعمل | ✅ تم إصلاحه هذا الجلسة | ⚠️ متبقٍ}.

### تجربة المستخدم
| البند | الحالة | ملاحظة |
|---|---|---|
| تسجيل/دخول/خروج | ✓ | bcrypt + تردّد + جلسة iron-session + logout في الهيدر |
| استعادة كلمة المرور | ✓ | مسار birthday → كلمة مرور جديدة عبر API؛ صفحة `/reset-password` (Firebase) غير موصولة لكنها خارج تدفق الموقع |
| الرئيسية | ✅ | إحصاءات الآن **مشتقة من بيانات حقيقية** (عدد الدورات/المقالات + متوسط التقييم + عدد المراجِعين بدل 1200/55/230 الثابتة)؛ مؤشر اليد والفتح مشروط بوجود رابط (sliders، معرض الصور)؛ بطاقات الدورات تنقل لتفاصيل الدورة بمعرّفها |
| الدورات | ✅ | **الدروس كانت لا تُرجع أبداً** (`include: chapters` فقط) → محتوى فارغ ولا paywall؛ الآن `chapters.include.lessons` + `gateCourseLessons` |
| المقالات/الكتب/الكوتشنغ | ✅ | **تنزيل الكتب كان معطلاً دائماً** (`fileUrl/pdfUrl` لم تُمرَّر من API)؛ الآن تُمرَّر ويُصحَّح شرط `handleDownload`. الكوتشنغ: gating + كاش خاص بدل `public s-maxage` |
| الفيديوهات | ✅ | روابط YouTube داخل `videoUrl` كانت تكسر `<video>` → كشف تلقائي لمعرّف YouTube وembed؛ إضافة `youtubeId` لقائمة الحساسة خادمياً |
| البودكاست | ✅ | نقر صامت عند تعطيل الشراء الفردي → CTA اشتراك (توجيه لصفحة الاشتراك) |
| PDFs | ✓ | حجب خادمي + فتح داخل الموقع |
| البث المباشر | ✅ | **``meetingUrl`` كان دائماً فارغاً** (الخريطة تقرأ `meetingUrl/videoUrl` بينما API يعيد `streamUrl/zoomUrl`) |
| العضوية/الحالة | ✓ | profile يعرض الاشتراك والانتهاء |
| الاشتراكات/الدفع | ✅ | **إزالة الأسعار الثابتة الوهمية** (`DEFAULT_PLAN_PRICES`, `DEFAULT_CCP "12345 67890 12"`): لا يُعرض مبلغ ولا زر إرسال حتى تصل الأسعار من الخادم، وإذا لم تكن مضبوطة تظهر رسالة تواصل مع الدعم؛ رقم CCP يُعرض فقط بعد جلبه من الإعدادات |
| الإشعارات | ✅ | إعادة توجيه للدخول لغير المسجّل (كانت صفحة فارغة صامتة) |
| اللغة | ✓ | تبديل ar/fr/en فوري |

### تجربة الأدمن
| البند | الحالة | ملاحظة |
|---|---|---|
| دخول/حماية | ✓ | جلسة + `X-Admin-Code` على كل الطرق عبر `verifyAdminAccess` |
| لوحة موحّدة `/admin?tab=` | ✓ | dashboard/content/payments/purchases/prices/homepage/settings جاهزة وحية |
| أقسام المحتوى | ✅ | تبويب البث كان يعرض قائمة فارغة (مفتاح استجابة خاطئ `liveSessions` بدل `sessions`)؛ حالة `published/draft` مرفوضة خادمياً → الآن تُرسم إلى `live/upcoming/ended` + حقل streamUrl + duplicate/bulk صحيحان |
| تعديل الشرائح | ✅ | **كان معطلاً 405** في 3 مواضع (`PUT /api/sliders` بدل `/api/sliders/{id}`) — أُصلح |
| إعدادات الرئيسية (Hero/أزرار/مقاطع) | ✓ | عبر HomepageCustomizer + `ALLOWED_SETTINGS_KEYS` |
| الروابط الاجتماعية | ✅ | **كانت لا تُحفظ إطلاقاً**: مفتاح `socialLinks` كان مرفوضاً من allow-list الإداري وغير معرّض علناً → أُضيف للمصدرين |
| الأعضاء | ⚠️ | تفعيل/تعطيل + إلغاء اشتراك متاحان؛ تعديل الدور لا يُخزَّن في schema (يعمل النظام على user/admin فقط) |
| الأسعار | ✓ | تعديل فوري من نفس مستند Firestore |
| المدفوعات/الشراءات | ✅ | **منع دورة الموافقة المزدوجة**: دفعة موافق عليها لا يمكن إعادة فتحها (كانت تمدّد شهراً كل مرة) |

### الحجب الخادمي والسلامة العامة
- المسودات غير مرئية لأي قائمة (`?status=` announced published) — متبقيٌ: تجاهل `?status=draft` الخبيث.
- ➖ **مسودات [id]**: مقالات/فيديوهات/بودكاست/PDF/دورات/كوتشنغ تُعيد 404 لغير الأدمن عند طلب مسودة مباشرة (`GET /[id]`).
- ➖ دروس الدورات الحساسة تُقص خادمياً عبر `gateCourseLessons`.
- ➖ قوائم coachings خاضعة الآن لنفس البوابة (كانت قديمة ومخزنة cache عامة).
- `session.ts` fail-closed في الإنتاج دون تغيير.

## توصيات/ملاحظات مؤجلة (لا تؤثر على إغلاق البنود الستة)
1. **كود الأدمن في localStorage** (`healing_space_admin_code` في `api-helpers`/`cloudinary-client`/HomepageCustomizer): تبقى لحصر الحلول الروجعة، لكن الجلسة الخادمية هي الأساس؛ ننصح بإزالتها نهائياً مستقبلاً. جمالية الأمان الأساسية في `session.ts` (fail-closed).
2. **إعادة توليد `.env`** بأمان قبل النشر الرسمي (Session + Service Account), وتحريك عبر CDN للـ Service Account إن أمكن.
3. ميزة "استعادة كلمة المرور" تعتمد بريد SMTP (بيئة حية فقط للاختبار الفعلي). صفحة `/reset-password` تُبقي عمداً — هي مسجلة في `app/page.tsx` و`store.ts` وتستهلك `/api/auth/reset-password` (Firebase oobCode)؛ إعادة تفعيلها الوظيفية متوقفة على إعداد SMTP في الإنتاج.
4. الدفع الفعلي/رفع Cloudinary/تحقق أدمن كامل يحتاج مفاتيح E2E حية — موثق للتركيب في بيئة الإنتاج.
5. قفل جلسة (خيار إضافي): التحقق من `userAgent` + IP داخل الجلسة لمنع إعادة اللعب.
6. عرض "scheduledAt" لأصناف غير live أُزيل من حمولة الحفظ (لا توجد جدولة خادمية لتلك الأصناف، والتحقق الصارم في live قد يرفض صيغة datetime-local) — يبقى الحقل للعرض/المعاينة فقط.

## الحالة
كل الوظائف الأساسية للمواصفات تعمل؛ المشروع يبني وينظّم (tsc rc=0، lint 27 errors+2 warnings = البازلاين، build rc=0). وأُغلقت ملاحظات الجولة الأخيرة:
- **تصلّب `admin-code`**: كود الإعداد الافتراضي `052307` مرفوض نهائياً في الإنتاج (`NODE_ENV === "production"` → false).
- **`admin-session`**: أُزيل التخزين الرمزي والإسوداد من `LoginPage` (بقي الكود الحقيقي فقط عبر جلسة الخادم).
- **تاريخ انتهاء الاشتراك**: يُحسب من أكبر `endDate` ويظهر كشارة فوق باقات `SubscriptionsPage`.
- **حقول SEO**: أصبحت `category/tags/metaTitle*/metaDesc*/ogImage` مقبولة في مخططات الإنشاء والتحديث للأنواع السبعة (courses/articles/videos/podcasts/pdfs/live/coachings) بما يطابق قوائم الحقول المسموحة في `db-security`.
- **`/api/content/*`**: تبين أنها غير موجودة أصلاً (لا ملفات ولا مراجع) — بند مقفل بلا حذف.
- **تعديل دور العضو في واجهة الأدمن**: مؤجل عمداً — المواصفة تطلب تفعيل/تعطيل فقط، ومنع منح صلاحية أدمن من واجهة غير محمية قرار أمني مقصود.